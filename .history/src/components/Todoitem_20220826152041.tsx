import { ITodo } from "../types/data"

 interface ITodoItem extends ITodo {}
 
 const TodoItem: React.FC = (props) => {
 const {id,title,complete}= 
 private _value : string;
 public get value() : string {
    return this._value;
 }
 public set value(v : string) {
    this._value = v;
 }
 

    return <div></div>
}
export {TodoItem}